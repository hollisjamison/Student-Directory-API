<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  link(rel="stylesheet" href="styles/styles.css")
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500&display=swap" rel="stylesheet">
  <title>Student Directory API</title>
</head>

<body>
  div(class="app-container")
    header
      h1 Student Directory
    div(class="app-container-inner")
      div(class="modify-container")
        form(class="editor add" action="/api/students" method="POST")
          h2 Directory Editor 
          div(class="choice-container")
            label I want to: 
            select(id="select" onchange="changeEditor()")
              option(value="add") add a student 
              option(value="update") edit a student
              option(value="delete") remove a student
          div(class="input-container add")
            label(for="add-name") Name:
            input(type="text" id="add-name" name="name")
            label(for="add-age") Age:
            input(type="number" step="1" min="14" max="100" id="add-age" name="age")
            label(for="add-address") Home Address:
            input(type="text" id="add-address" name="address")
            label(for="add-gpa") GPA:
            input(type="number" step="0.01" min="0" max="4.00" id="add-gpa" name="gpa")
            label(for="add-major") Major:
            input(type="text" id="add-major" name="major")
            label(for="add-image") Image:
            input(type="text" id="add-image" name="image")
          div(class="input-container update")
            label(for="update-name") Name:
            input(type="text" id="update-name")
            label(for="update-age") Age:
            input(type="number" step="1" min="14" max="100" id="update-age")
            label(for="update-address") Home Address:
            input(type="text" id="update-address")
            label(for="update-gpa") GPA:
            input(type="number" step="0.01" min="0" max="4.00" id="update-gpa")
            label(for="update-major") Major:
            input(type="text" id="update-major")
            label(for="update-id") Student ID:
            input(type="text" maxlength="6" id="update-id")
          div(class="input-container delete")
            label(for="delete-id") Student ID:
            input(type="text" maxlength="6" id="delete-id")
            
          div(class="submit-container")
            button(type="submit") Submit Change
            

      div(class="students-container")
        for student in students
          div(class="profile")
            h2= student.name
            img(class="avatar" src="/assets/" + student.image)
            div(class="data-list")
              h3= "Age: " + student.age
              h3= "Home Address: " + student.address
              h3= "GPA: " + student.gpa
              h3= "Major: " + student.major
              h3= "Student ID: " + student.id.toString().padStart(6, "0")
              div(class="separation")
  script(src="scripts/editor.js")
</body>

</html>